require 'spec/spec_helper'
require '<%= ActiveSupport::Inflector.underscore(site_name) %>'

describe "<%= ActiveSupport::Inflector.camelize(file_name) %>" do
  it "should fail because you haven't written any tests yet" do
	false.should be_true
  end
end