require 'lib/sites/<%= Inflector.underscore(site_name) %>'
require 'spec'

describe "<%= Inflector.camelize(file_name) %>" do
  it "should fail because you haven't written any tests yet" do
	false.should be_true
  end
end