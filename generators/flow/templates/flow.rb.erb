require 'rubygems'
require 'taza/flow'

module <%= site_name.camelize %>
  class <%= name.camelize %> < ::Taza::Flow
    alias :<%= site_name.underscore %> :site

    def run(params={})
      
    end
  end
end
