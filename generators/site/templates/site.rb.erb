require 'rubygems'
require 'taza'

module <%= name.camelize %>
  def self.new(params={}, &block)
		    <%= name.camelize %>.new(params,&block)
  end
  class <%= name.camelize %> < ::Taza::Site

  end
end
